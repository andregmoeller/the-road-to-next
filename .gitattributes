# Treat all files as “auto,” but enforce LF (Linux) line breaks in the repository.
* text=auto eol=lf

# Ensure that the lock file always has LF, as it is often generated by tools.
package-lock.json text eol=lf
